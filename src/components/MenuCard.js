import React from 'react';
import '../styles/MenuCard.css';

const MenuCard = () => {
    return (
        <div className="menu-card-container">
            <h1>Menu Card</h1>
            {/* Add your categories, food items, and bill summary here */}
        </div>
    );
};

export default MenuCard;
